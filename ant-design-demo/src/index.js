import React from 'react';
import {render} from 'react-dom';
import App from './app.jsx';
import 'antd-mobile/dist/antd-mobile.css';
//import from node modules, so the path is correct
render(<App/>,document.getElementById('root'));
